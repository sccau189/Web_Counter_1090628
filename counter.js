$(document).ready( () => {

    // var 變數 = 資料
    var offset = $("#test").offset();     //取的元素位移資料
    var top = offset.top;                 //上方
    var left = offset.left;               //下方
    
    
});